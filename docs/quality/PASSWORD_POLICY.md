# Basic Auth Password Policy

When remote Basic Auth is enabled, `IPV6_SENTINEL_PASSWORD` must contain at least 12 characters.

Short passwords are rejected during application startup. Localhost execution with Basic Auth disabled is unchanged.
